<div align="center">

<img src="docs/icon.png" alt="豆沙包调用器" width="100" />

# 豆沙包调用器

**基于 RikkaHub 二次开发的安卓 AI 聊天客户端**

上游基线：RikkaHub **2.5.1** ｜ 本版：**2.5.1-mod69**（versionCode 302）｜ 包名：`me.rerere.rikkahub.mod`

</div>

---

## 这是什么

在开源项目 [RikkaHub](https://github.com/rikkahub/rikkahub) 基础上做的增强版：保留原有全部能力，重点打磨**长时间任务、多模型协作、多账号多密钥**这三类实际场景。

包名与官方版不同，**能和官方版同时安装**，数据互不影响。仓库里是**完整可编译源码**（不是补丁）。

> ⚠️ 个人维护，**与 RikkaHub 官方无关**；遇到问题请在本仓库反馈，别去找官方。

## 新增功能（官方版没有）

| 功能 | 一句话说明 |
|---|---|
| **自动续跑** | 回复写到一半被掐断（撞上限 / 断网 / 报错 / 没说完就停）会自动接着写完；两种方式可切换（发"继续" / 重新生成最后一段）；次数 0~99 可设；带真实错误原文；程序级去重防复读 |
| **圆桌会议** | 多模型各出方案、互相反驳、主模型拍板；每个座位可进去看思考与工具调用 |
| **子代理** | 主模型派活给子代理、只回一份摘要；角色可分派模型；写权限白名单（无 shell / 编译 / 删除） |
| **官方账号登录** | Gemini / ChatGPT / Grok 直接登录官方账号，不用填 Key；多账号轮询；凭据加密只存本机 |
| **对话记住模型** | 每个对话记住自己最后用的模型，切回来自动恢复（照 Chatbox），默认开 |
| **本对话 token 明细 + 算花费** | 顶栏常驻消耗、过程中实时刷新、带缓存命中率；填单价自动估算本对话花费 |
| **自动压缩** | 到触发值自动压缩、先建分支保留原对话；触发值可自定、可按模型单独设；占用条常显 |
| **重点标色（红/黄/绿）** | 默认关、零注入；打开只给模型一段中性说明，可用可不用 |
| **强制翻阅技能** | 默认关；打开后模型首次回答前必须先翻阅一遍已启用技能的说明（SKILL.md）再动手，不会漏用；没开技能时完全不注入 |
| **多密钥 / 多账号轮询** | 一把报错自动换下一把、整池轮完；按报错文字识别限额；吐过内容绝不重发 |
| **从官方版迁入工作区** | 把官方版里的工作区文件整树搬过来，断点续传、只读不写入 |

## 优化与增强（官方已有，我们加强）

| 功能 | 加强了什么 |
|---|---|
| **手动压缩** | 新增「智能模式」（官方旧版照旧可选），记住上次选择 |
| **工作区** | 命令默认**不限时**，可自定义超时秒数 |
| **备份与恢复** | 修好"导入被页面回收打断还误报失败"；导出中断不留半个坏包；也能从 ChatBox / Cherry Studio 导入 |
| **输入面板与文件** | 附件条重做、文件选择器、视频内嵌播放、工作区图片直显、单次上限 256MB → 10GB、批量删除 |
| **渠道兼容修复** | 商汤（DeepSeek 空回根因；kimi 屏蔽询问卡片）、询问卡片容错 + 跳过按钮、Exa 搜索分段读取、Gemini 429、Grok 额度/冷却、流式兜底 |
| **其它** | 应用名「豆沙包调用器」+ 自定义图标 + 默认头像；删 QQ 群 / Discord / 赞助页 |

## 自己编译

需要 JDK 17 + Android SDK（compileSdk 37），仓库自带 Gradle wrapper：

```bash
./gradlew :app:assembleDebug      # 出能装的测试包
./gradlew :app:assembleRelease    # 出正式包（需自备签名）
```

- 签名从根目录 `local.properties` 读取；没有也能编译，但产出是未签名的包；
- `material3/material-color-utilities` 源码已直接包含，无需拉取子模块；
- 仓库内置占位用的 `app/google-services.json`（不连任何真实服务），开箱可编译；
- Web UI 静态资源是构建产物，需要时 `./gradlew :web:buildWebUi` 生成。

## 注意事项

- 个人二改版，与 RikkaHub 官方无关；
- **不会联网检查更新**，新版本到本仓库 Releases 页面拿；
- 与官方版两套独立数据，可共存但**不能互相覆盖安装**；
- 官方账号通道（Gemini / Codex / Grok）有账号风险，请自行评估。

## 致谢与协议

- 上游：[RikkaHub](https://github.com/rikkahub/rikkahub)（AGPL-3.0）；本仓库同样以 **AGPL-3.0** 发布（见 LICENSE）。
- 基于本仓库二次修改并对外发布时，请同样公开完整源码并保留本说明。
- 感谢上游作者与所有贡献者。
